[user]
	name = Greg Heartsfield
	email = scsibug@imap.cc
	signingkey = B777FE08F8CE28E9149230DDC7F4AA6B95F11E3A
[core]
	editor = emacsclient
	excludesFile = ~/.gitignore
[init]
	defaultBranch = master
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
[commit]
	template = ~/.gitmessage
[merge]
	conflictstyle = diff3
[credential]
	helper = store
[push]
        autoSetupRemote = true
[alias]
	lola = log --graph --pretty='format:%C(auto)%h %d %s %C(green)(%ad) %C(bold blue)<%an>' --abbrev-commit --all --date=relative
	lol = log --graph --pretty='format:%C(auto)%h %d %s %C(green)(%ad) %C(bold blue)<%an>' --abbrev-commit --date=relative
